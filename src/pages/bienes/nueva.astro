---
import Layout from '@/components/Layout.astro';
import Icon from '@/components/Icon.astro';
import GoodCreateForm from '@/components/GoodCreateForm.tsx';
import { deployment } from '@/config/deployment';

const { lang, t } = Astro.locals;
---

<Layout title={t('goods.new.title')} description={t('goods.new.intro')}>
  <a href="/bienes" class="-ml-1 inline-flex items-center gap-1 text-sm text-fg-tertiary transition-colors hover:text-fg">
    <Icon name="back" size={16} />
    {t('goods.list.title')}
  </a>

  <h1 class="mt-2 text-xl font-semibold tracking-tight text-fg">{t('goods.new.title')}</h1>
  <p class="mt-1 text-sm text-fg-tertiary">{t('goods.new.intro')}</p>

  <div class="mt-5">
    <GoodCreateForm client:load lang={lang} defaultZone={deployment.defaultZone} />
  </div>
</Layout>
